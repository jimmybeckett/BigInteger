#include "../../BigInteger.h"

namespace euler {
    BigInteger BigInteger::operator+(const BigInteger& b) const {
    /*
        if (this* == 0) 
            return b;
        if (b == 0) 
            return *this;
        if (*this < 0 && b > 0)
            return b - *this;
        if (*this > 0 && b < 0)
            return *this - b;
        if (*this < 0 && b < 0) 
            return -(*this + b);
     */   
        BigInteger temp;
        return temp;
    }
}
